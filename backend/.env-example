PORT=4000                  # Port number for the backend server
MONGODB_URI=mongodb://localhost:27017/teritage-inheritance  # MongoDB connection URI
JWT_SECRET=secret          # Secret key for signing JWT tokens
JWT_EXPIRES_IN=7d          # JWT token expiration duration (e.g., 7 days)
EMAIL_FROM=                # Default sender email address for outgoing emails
SMTP_HOST=                 # SMTP server host for sending emails
SMTP_PORT=587              # SMTP server port (587 is common for TLS)
SMTP_USER=                 # SMTP username for authentication
SMTP_PASS=                 # SMTP password for authentication
HEDERA_API_BASE_URL=       # Base URL for Hedera API integration
CONTRACT_ADDRESS=          # Smart contract address (for blockchain interactions)
CONTRACT_RPC_URL=          # RPC URL for connecting to the blockchain network
